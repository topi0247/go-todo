development:
    dialect: postgres
    dir: migrations
    datasource: host=db user=postgres dbname=webapp sslmode=disable password=password
    table: migrations

production:
  dialect: postgres
  datasource: "host=$DB_HOST user=$DB_USER dbname=$DB_NAME password=$DB_PASSWORD sslmode=disable"
  dir: migrations
  table: migrations
